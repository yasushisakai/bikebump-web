// @flow
import React from 'react';

type Props = {

};

export default function MapCommutes (props: Props) {
    const style = {width: '100%', height: '100%'};
    return (
        <div style={style}>
            <div id='mapCommutes' style={style} />
        </div>
    );
}
